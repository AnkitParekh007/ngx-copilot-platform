# Good First Issues

1. Add a signal-based host app example that wraps `CopilotShellComponent`.
2. Add mobile layout screenshots for the copilot shell and timeline.
3. Add keyboard interaction tests for the approval card.
4. Add a richer mock RAG adapter fixture with multiple source types.
5. Add a markdown API surface table generated from `public-api.ts`.
6. Add a “how to migrate from app-specific copilot code” guide.
7. Add an Angular Material theme example for the exported components.
8. Add a PrimeNG shell wrapper example without changing core SDK abstractions.
9. Add a packaging smoke test against `dist/ngx-copilot-sdk`.
10. Add a Storybook or preview plan doc without publishing the package yet.
