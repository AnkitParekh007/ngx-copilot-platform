/** Supported theme values. */
export type Theme = 'light' | 'dark' | 'system';

/** All possible resolved (actual rendered) themes. */
export type ResolvedTheme = 'light' | 'dark';

export const THEME_STORAGE_KEY = 'ngx-copilot-sdk:theme';
