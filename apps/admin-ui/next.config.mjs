/** @type {import('next').NextConfig} */
const nextConfig = {
  // Admin UI proxies API calls to packages/backend
  // Set NEXT_PUBLIC_BACKEND_URL=http://localhost:3001 in .env.local
};

export default nextConfig;
